const Story = () => {
  return (
    <section>
      Story To implement Rotate Element like one in the bento cards so it could
      be reusable. To extract it and use it as a wrapper
    </section>
  );
};

export default Story;
